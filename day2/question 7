#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if (n < 0) n = -n;

    int product = 1;
    if (n == 0) product = 0;
    while (n > 0) {
        product *= n % 10;
        n /= 10;
    }

    cout << "Product of digits = " << product;
    return 0;
}

